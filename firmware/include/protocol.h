#ifndef PROTOCOL_H
#define PROTOCOL_H

// Shared definitions for the WebUSB binary message protocol.
// Authoritative wire format spec: docs/protocol.md.
// Must stay in sync with webui/src/protocol/messages.ts.

#include <stdint.h>

// TODO: message id enum, framing struct, version constant.

#endif
