# Placeholder. Replace with the canonical pico_sdk_import.cmake from the
# Pico SDK so CMake can locate the SDK:
#
#   cp "$PICO_SDK_PATH/external/pico_sdk_import.cmake" .
#
# Until then, configure will fail with "include could not find load file".
