// Reactive store of per-controller state (inputs, battery, signal),
// populated by parsed messages from useWebUsb.

export function useControllers() {
  // TODO: reactive map keyed by player index.
  return {}
}
