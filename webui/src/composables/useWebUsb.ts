// Single source of truth for the WebUSB connection: requestDevice,
// open/claim/select, transferIn/transferOut framing, reconnection,
// and error paths (unplug mid-session, permission denied).

export function useWebUsb() {
  // TODO: reactive connection state, connect(), disconnect(), send(), onMessage().
  return {}
}
