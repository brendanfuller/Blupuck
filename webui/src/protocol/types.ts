// TypeScript types for the WebUSB binary protocol.
// Authoritative wire format spec: ../../docs/protocol.md.
// Must stay in sync with firmware/include/protocol.h.

// TODO: MessageId enum, ControllerState, BatteryLevel, RemapTable, Profile, etc.
export {}
